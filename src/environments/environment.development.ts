export const environment = {
  api: {
    host: "http://localhost:8000/api/user",
    path: {
      auth: "/auth",
      device: "/device",
      deviceData: "/device/data",
      dashboard: "/dashboard/",
      order: "/order",
      cart: "/cart",
    }
  }
};
