export const environment = {
  api: {
    host: "http://localhost:8080/api/user",
    path: {
      auth: "/auth",
      device: "/device",
      deviceData: "/device/data",
      dashboard: "/dashboard/",
      order: "/order",
      cart: "/cart",
    }
  }
};
